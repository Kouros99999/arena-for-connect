{
  "name": "arena-agent-events",
  "version": "0.1.0",
  "private": true,
  "description": "Arena ingest Lambda for the Amazon Connect agent event stream",
  "main": "handler.js",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
